from setuptools import setup

setup(name='factorial',
      version='0.1',
      description='Simple factorial program',
      author='Ethan Hansen',
      packages=['factorial'],
      zip_safe=False)
