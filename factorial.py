#!/usr/bin/python

#calculates the factorial of a number

def factorial(number):
	if (number > 0) :
    		while (counter > 1) :
        		total *= counter
        		counter -= 1
    		return total
	else:
    		print("that is an invalid input")
		return 0
